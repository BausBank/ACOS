"""ACOS trust layer - entity-agnostic verifiability primitives.

Packages: ``core.journal`` (commit-reveal decision journal, hash chain),
``core.anchor`` (dual anchoring: Arc + Bitcoin/OpenTimestamps),
``core.verify`` (open verifier: recompute P&L from public fills),
``core.site`` (public snapshot emitter with whitelist redaction).
"""
